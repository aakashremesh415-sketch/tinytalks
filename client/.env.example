# Copy to .env for local dev. On Vercel, set these as Project Environment
# Variables (Settings → Environment Variables) — they get baked into the
# client build, so they must start with VITE_ to be exposed by Vite.

VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER="us2"
